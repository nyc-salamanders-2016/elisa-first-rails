<h1>Create A New Post</h1>
<%= form_for :post, url: '/posts/new', :html => {:class => 'new-post-form'} do |f| %>
  Title: <%= f.text_field :title %><br>
  Body: <%= f.text_area :body %><br>
  <%= hidden_field_tag(:user_id, current_user.id) %>
  <%= f.submit "Submit" %>
<% end %>
